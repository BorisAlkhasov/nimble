const AppartmentModel = {
  zpid, // not on page, but in object
  address,
  city,
  state,
  zip,
  sqft,
  baths,
  beds,
  lat,
  lng,
  rent,
  zrent, // estimated
  url,
  description,
  price_history, // can't find
  days_on_zillow,
  contacts,
  // agent_name, // how to know if this is a business?
  // agent_business_name,
  // agent_type,
  // agent_info,
  // applications,
  // owner,
};
